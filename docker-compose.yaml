version: '3.8'

services:
  bot:
    build: .
    container_name: helicopter_bot
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

